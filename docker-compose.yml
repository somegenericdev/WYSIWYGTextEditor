version: '3.4'

services:
  image-api:
    image: ${DOCKER_REGISTRY-}sampleimageapi
    build:
      context: .
      dockerfile: samples/SampleImageApi/Dockerfile

  blazor-serverside:
    image: ${DOCKER_REGISTRY-}blazorserverside
    build:
      context: .
      dockerfile: samples/BlazorServerSide/Dockerfile

